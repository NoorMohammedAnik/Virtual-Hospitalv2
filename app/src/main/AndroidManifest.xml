<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rr.virtual_hospital">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".VbActivity" />
        <activity android:name=".FirstAidActivity" />
        <activity android:name=".CallActivity" />
        <activity android:name=".HospitalActivity" />
        <activity android:name=".DiaActivity" />
        <activity android:name=".NearestActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HospGoActivity"></activity>
    </application>

</manifest>